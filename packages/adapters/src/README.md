# Adapter source

Each child directory contains one external-system interface and its initial mock implementation. Transport-specific implementations may be added later without leaking into domain code.
