# Contracts

This package is the source of truth for OpenAPI 3.1 API definitions and versioned event schemas. API implementation starts only after the relevant endpoint is defined here.

Validate the API contract from the repository root with pnpm contract:lint.
