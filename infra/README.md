# Local infrastructure

Docker Compose starts PostgreSQL 16, NATS with JetStream enabled, Redis, and Keycloak in development mode. Credentials are intentionally local-only defaults and must never be reused outside developer machines.

Run Docker Compose from the repository root with the compose file in this directory.
