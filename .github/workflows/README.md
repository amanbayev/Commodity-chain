# GitHub Actions workflows

Continuous integration is split into ordered lint, contract, migrate-check, typecheck, test, and build jobs so each gate is visible and enforceable. Ledger tests run against a migrated PostgreSQL 16 service.
